specVersion: "v1beta"
serviceId: "CampusThrifts"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "CampusThrifts-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
